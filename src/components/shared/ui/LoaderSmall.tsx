import React from "react";
import { LoaderProps } from "../../../library/types";

const LoaderSmall: React.FC<LoaderProps> = () => {
  return <div className={`loader`} />;
};

export default LoaderSmall;
